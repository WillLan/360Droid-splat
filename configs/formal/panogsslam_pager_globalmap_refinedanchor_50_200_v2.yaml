base_config: panogsslam_pager_globalmap_refinedanchor_50_200_v1.yaml

# Dataset-specific sky and voxel policies are applied by the v2 campaign
# manifest. This shared base keeps all weights and the confirmed
# PaGeR + SphereGlue + Global-Map-Sim3 + CAMERA50/JOINT200 mainline unchanged.
WeightsAndBiases:
  group: panogsslam-formal-pager-globalmap-refinedanchor-50-200-v2
